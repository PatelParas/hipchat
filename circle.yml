dependencies:
  pre:
    - go get github.com/andybons/hipchat
  override:
  # Remove cached Go sources
  - rm -rf $HOME/.go_workspace
test:
  override:
  - go vet ./...
  - go test ./...
experimental:
  notify:
    branches:
      only:
        - master
